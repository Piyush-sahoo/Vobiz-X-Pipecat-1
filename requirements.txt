# Pipecat AI 1.8.x. Requires Python >= 3.11.
#
# Extras map to the services bot.py uses:
#   google    - Gemini LLM (cascaded) and Gemini Live (realtime)
#   deepgram  - STT and TTS (cascaded)
#   silero    - local VAD / turn detection
#   websocket - the FastAPI WebSocket transport Vobiz streams into
pipecat-ai[google,deepgram,silero,websocket]>=1.8.1,<1.9

# Vobiz telephony: the <Stream> WebSocket protocol serializer.
pipecat-vobiz>=0.0.3,<0.1

# Web framework
fastapi
uvicorn
python-multipart

# HTTP clients
aiohttp
requests

# Environment variables
python-dotenv

# Logging
loguru
